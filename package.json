{
  "name": "make-webhook-proxy",
  "version": "1.0.0",
  "description": "Node.js proxy server for Make.com webhook — bypasses CORS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
